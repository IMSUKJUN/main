# Claude Page View

Claude 대화의 세로 스크롤을 가로 페이지 구조로 바꿔 보여주는 크롬 확장 프로그램.

카드 한 장이 페이지 한 장이고, 한 페이지는 화면 안에 온전히 들어간다. 위아래로 잘려
나가는 부분이 없다.

## 지금 되는 것

- 대화 전체를 가로 카드 트랙으로 그린다
- 1차로 샷(질문 하나 + 답변 하나) 단위, 2차로 길이 단위로 페이지를 나눈다
- 문단·목록·제목·표·코드블록을 페이지 경계에서 자르지 않는다
- 프롬프트는 **가운데 한 개만** 떠 있다. 페이지를 넘겨도 그 자리에 있고, 다른 샷으로 넘어갈 때만 바뀐다
- 프롬프트는 한 줄로 접히고, 펼치면 아래 카드 **위에 겹쳐서** 펼쳐진다 (카드는 안 밀림)
- 카드 폭은 원래 스크롤 화면의 글줄 폭과 같다. 줄바꿈이 스크롤 화면과 같아진다
- 도구 실행 내역은 접힌 한 줄로 두고, 펼치면 **본문 오른쪽에** 페이지로 붙는다 (`T1`, `T2`…)
- 세션 상태(`세션 재개됨` 등)는 카드 사이에 칩으로 세운다
- 카드마다 얇은 선과 연한 그림자가 붙어 페이지끼리 구분된다
- 중앙 카드만 또렷하고 좌우는 바깥으로 갈수록 흐려진다
- 이동: 휠 한 틱에 한 페이지 / 드래그 / 좌우 카드 클릭 / 좌우 카드 클릭 유지 /
  좌우 화살표 / 아래쪽 스크롤바 끌기(손잡이 한 칸 = 한 페이지)
- 입력창은 덮지 않는다. 화면 아래 입력창을 찾아 그 위까지만 덮는다
- 다른 대화로 옮기면 페이지 보기가 저절로 꺼진다
- 스크롤바 길이는 카드(= 입력창) 폭과 같다
- 가운데 아래에 `4 / 21 · 4p` 로 지금 위치가 나온다
- 페이지 번호는 카드 안 **좌하단**
- 켤 때 대화가 다 실려 올 때까지 기다렸다 읽는다. 읽는 도중에 늘어나면 다시 읽는다

## 설치

1. 크롬에서 `chrome://extensions` 열기
2. 오른쪽 위 **개발자 모드** 켜기
3. **압축해제된 확장 프로그램을 로드합니다** 를 눌러 이 폴더를 고르기
4. claude.ai 에서 오른쪽 아래 **페이지 보기** 버튼 또는 `Alt+P`

`Esc` 로 원래 스크롤 화면으로 돌아온다.

## 어떻게 동작하나

claude.ai 대화창은 두 가지 성질이 있고, 이게 구조를 결정했다.

**앱 DOM 은 읽기만 한다.** React 가 만든 노드를 다른 곳으로 옮긴 뒤 React 가 그것을
지우려 하면 `removeChild` 오류가 나면서 메시지가 통째로 사라진다. 그래서 원본 대화는
손대지 않고, 읽어서 우리 레이어에 다시 그린다.

**대화창은 가상 스크롤이다.** 화면 밖 행은 DOM 에 없다(실측: 44행 중 4~15행만 존재).
그래서 켤 때 맨 위로 올라간 뒤 차례로 내려오며 복제한다(`src/harvest.js`).
정해진 픽셀 간격으로 내려가면 앱이 아직 안 그린 구간을 지나치므로, **마지막으로 본 행이
화면 맨 위에 오도록** 옮긴다. 앱이 그린 만큼만 전진하니 건너뛸 수가 없다.

**페이지 나눔은 CSS 다단이 한다.** 한 덩어리를 `column-count` 로 흘리면 브라우저가
줄 단위로 정확히 나눠 준다. 카드 테두리는 `box-decoration-break: clone` 이 조각마다
온전히 그려 주기 때문에, 페이지 수만큼 요소를 복제할 필요가 없다.

**코드블록은 shadow DOM 이라 복제하면 비어 있다.** `.epitaxy-diff` 의 `data-code-text`
속성에 원본 코드가 그대로 남아 있어서 그것으로 되살린다(`src/source.js`).

## 파일

| 파일 | 하는 일 |
|:--|:--|
| `src/config.js` | 비율·간격·시간 같은 설정값, 셀렉터 |
| `src/source.js` | 앱 DOM 읽기 (행 식별, 프롬프트/도구 문구, 복제) |
| `src/harvest.js` | 가상 스크롤을 훑어 행 수집 |
| `src/view.js` | 카드 트랙 그리기, 페이지 계산, 겹쳐 그리는 층 |
| `src/nav.js` | 휠·드래그·클릭·클릭 유지 |
| `src/content.js` | 켜고 끄기, 변화 감시 |
| `src/overlay.css` | 카드·말풍선·초점 표현 |
| `test/mock.html` | claude.ai 대화창을 같은 구조로 모사한 화면 (가상 스크롤 포함) |
| `test/run.mjs` | 확장을 크로미움에 얹고 모사 화면에서 검사 |

## 시험

```bash
node test/run.mjs
```

모사 화면(행 27개 · 샷 8개, 전체 높이 27,271px, 처음 마운트된 행 4개)에서 확인되는 값이다.
모사에는 실제 화면과 같은 조건을 넣어 두었다. 애니메이션 스크롤, 한 박자 늦게 붙는 행,
맨 아래로 끌어당기는 동작, 색이 아닌 HSL 숫자 변수, 사용자와 답변의 다른 `entry-index`,
대화창 위에 떠 있는 입력창, 그 자리에 가야만 붙는 행, **처음에는 끝쪽 10행만 실려 있고
오래된 것이 0.4초마다 6행씩 뒤늦게 들어오는 것**.

| 항목 | 결과 |
|:--|:--|
| 카드 폭 | 822 = 글줄 폭 768 + 좌우 안여백 |
| 만들어진 페이지 | 49장 |
| 페이지 밖으로 나간 글자 | 1,911줄 중 **0줄** |
| 페이지 경계에 걸친 글자 | **0줄** |
| 초점 카드 가운데 정렬 | 중앙에서 0px |
| 프롬프트 말풍선 | 1개, 화면 중앙에서 0px |
| 수집한 행 | 27개, 빠진 번호 **0개** (0-26) |
| 샷 수 | 8개 = 모사의 샷 8개 |
| 못 읽은 줄 표시 | 없음 (스스로 다 주움) |
| 덜 실린 채로 읽으면 | 샷 7/8, 페이지 61장 (기다림을 끄고 잰 값) |
| 다 실린 뒤 읽으면 | 샷 8/8, 페이지 49장 |
| 액션 바 | 0개 (없앰) |
| 페이지 번호 자리 | 카드 왼쪽에서 18px, 전부 왼쪽 |
| 마지막 페이지 높이 | 749 = 카드 높이 749, 어긋난 페이지 0개 |
| 모사 조건 | 애니메이션 스크롤 + 220ms 지연 렌더 + 맨 아래 끌어당김 + 좁은 자리에서만 붙는 행 |
| 스크롤바 | 폭 822 = 카드 폭 822, 가운데 정렬 |
| 휠 | 한 틱에 한 페이지 (0 → 1, 네 틱 → 4) |
| 입력창 | 덮개 아래끝 917 < 입력창 위끝 925, 안 가림 |
| 다른 대화로 이동 | 덮개 제거됨, 버튼 `페이지 보기` 로 복귀 |
| 페이지 카운터 | `1 / 49 · 1p` |
| 코드블록 복원 | 5개, 빈 `diffs-container` 0개 |
| 프롬프트 펼침 | 카드를 13px 덮고, 카드는 제자리 |
| 도구 펼침 | 본문 바로 오른쪽에 붙음, 페이지 49 → 50 |
| 끈 뒤 | 덮개 제거됨, 원본 대화 그대로 |
| 카드 그림자 | `0 1px 2px`, `0 10px 28px` 조각마다 적용 |
| 덮개 배경 | `rgb(250, 249, 245)` 불투명 |
| 원본 대화 | `visibility: hidden` |
| 화면 중앙에 잡히는 것 | 페이지 보기 레이어 |
| 콘솔 오류 | 없음 |

`test/shots/` 에 단계별 화면이 남는다.

## 고친 것

**0.1.9** — 네 가지.

**켤 때 대화가 다 실려 올 때까지 기다린다.** 버튼을 일찍 누르면 앱이 아직 기록을
받아오는 중이라 끝쪽 몇 행밖에 없다. 그 상태로 읽으면 앞부분이 통째로 빠진다.
게다가 행 번호가 "지금 실려 있는 목록 안에서의 자리"라면, 오래된 것이 들어올 때마다
같은 행의 번호가 밀린다. 그러면 빠졌다는 사실조차 알 수 없다.
그래서 전체 행 수·대화창 높이·붙어 있는 행 수 세 가지가 연속 세 번 안 변할 때까지
기다렸다 시작한다(최대 12초, 그 뒤에는 그냥 시작한다). 기다리는 동안 주워 둔 것은 버린다.
읽는 도중에 전체 행 수가 늘면 자리가 전부 밀린 것이므로 앞서 읽은 것을 버리고
처음부터 다시 읽는다(두 번까지). 화면에는 그동안 `대화를 불러오는 중…` 이 뜬다.

모사 화면에 이 조건을 넣어 회귀를 잡는다. 기다림을 끄면 8개 샷 중 7개만 나오고
페이지가 49장에서 61장으로 늘어난다(같은 행을 두 번 읽은 결과). 기다리면 8/8, 49장이다.

**액션 바를 없앴다.** 모양만 있고 원본 버튼과 연결되지 않은 상태였다.

**페이지 번호를 왼쪽으로.** 카드 안 우하단에서 좌하단으로 옮겼다(카드 왼쪽에서 18px).

**마지막 페이지도 카드 높이 그대로.** 다단으로 흘리면 마지막 조각은 내용만큼만
높이를 갖는다. 이제 카드 높이를 `페이지 수 × 카드 높이` 로 못박아 마지막 페이지도
앞 페이지와 같은 크기다.

**0.1.8** — 네 가지.

**대화 읽기를 "본 만큼 전진"으로.** 맨 위로 올라간 뒤 내려오는 것은 같지만, 내려가는
방식을 바꿨다. 예전에는 화면 높이의 0.9배씩 정해진 간격으로 내려갔다. 앱이 그 구간을
아직 안 그렸으면 그대로 지나친다. 이제 **마지막으로 본 행이 화면 맨 위에 오도록** 옮긴다.
앱이 그린 만큼만 전진하므로 건너뛸 수가 없다. 맨 위에 닿았는지도 0번 행이 붙었는지로
확인한다.

**프롬프트를 가운데 한 개로.** 페이지마다 말풍선을 붙이던 것을 가운데 하나만 띄우도록
바꿨다. 페이지를 넘겨도 그 자리에 있고, 다른 샷으로 넘어갈 때만 내용이 바뀐다.

**스크롤바 길이를 카드 폭에 맞춤.** 화면 전체 폭이 아니라 카드(= 입력창) 폭과 같고
가운데 정렬이다.

**다른 대화로 옮기면 저절로 꺼진다.** 주소가 바뀌는지 지켜보다가 대화가 달라지면 끈다.

**0.1.7** — 네 가지.

**입력창이 안 뜨던 문제.** 채팅·coworker 화면은 입력창이 대화창 위에 떠 있는 배치라
덮개가 그 위를 덮어 버렸다. 이제 입력창 상자를 찾아 그 위쪽까지만 덮는다.
`contenteditable`, `textarea`, `*-prompt-input` 중 화면 아래쪽에 있는 것을 찾고,
버튼까지 포함한 바깥 상자를 거슬러 올라가 잡는다.

**스크롤 길이를 페이지 단위로.** 손잡이 길이가 픽셀 비율이 아니라 `막대 폭 ÷ 페이지 수`다.
손잡이 하나가 한 페이지고, 한 칸 옮기면 한 페이지 넘어간다.

**휠 한 틱에 한 페이지.** 연속 스크롤은 한 페이지 넘기는 데 한참 굴려야 했다.
한 틱이 한 페이지고, 220ms 안에 들어온 휠은 한 번으로 친다.

**카드 폭을 채팅·coworker 쪽에 맞춤.** 화면마다 글줄 폭이 다르다(Claude Code 쪽이 넓다).
768px 상한을 두어 어느 화면에서나 채팅 쪽 폭이 된다.

**0.1.6** — 못 읽은 줄을 프로그램이 스스로 다시 읽는다.

0.1.5 까지는 못 읽은 줄이 있으면 그 사실만 알렸다. 이제 켠 뒤에 빠진 줄이 있으면
사용자가 시키지 않아도 두 번까지 스스로 다시 줍고, 줍는 동안 화면을 갱신한다.
그래도 남으면 오른쪽 아래에 `못 읽은 줄 2개 · 다시 읽기` 가 뜨고, 눌러서 한 번 더
시도할 수 있다. 아무 문제 없으면 아무것도 안 뜬다.

다시 줍는 방식도 고쳤다. 빠진 행의 자리 한 곳만 찍던 것을 그 행 주변 네 지점을
차례로 시도하도록 바꿨다. 어느 자리에서 행을 붙일지는 앱이 정하기 때문이다.
그리고 자리를 옮긴 직후에는 이전 자리의 행이 아직 남아 있어 "채워졌다"로 보이므로,
최소 세 번은 확인한 뒤에 다음 칸으로 넘어간다.

모사 화면에 "그 자리에 정확히 가야만 붙는 행"을 하나 심어 이 과정을 시험한다.

**0.1.5** — 대화를 다 못 읽던 문제의 진짜 원인.

수집은 스크롤을 한 칸씩 내리며 그때 붙어 있는 행을 복제하는 방식이다. 문제는 "언제
다음 칸으로 넘어갈지"였다. 처음에는 150ms 고정으로 기다렸는데, 앱은 스크롤 직후가
아니라 한 박자 뒤에 행을 붙이고 그 시간이 일정하지 않다. 다음으로 "붙어 있는 행이
더 안 바뀌면 넘어간다"로 바꿨더니 더 나빠졌다. **앱이 아직 시작도 안 했을 때도 안
바뀌기 때문**이다. 모사 화면에 220ms 지연을 넣자 27행 중 5행만 읽혔다.

이제 **지금 보이는 자리에 행이 실제로 걸쳐 있는지**를 보고 넘어간다. 시간에 기대지
않으므로 앱이 느리든 빠르든 같은 결과가 나온다. 같은 모사에서 27행 전부를 읽는다.

같이 고친 것: 애니메이션 스크롤(`html.scroll-smooth`)을 수집 동안 꺼서 위치를 정확히
짚게 했고, 맨 아래로 끌어당기는 동작(`data-autoscroll-container`)에 밀리면 다시 밀어
넣는다. 빠진 곳 검사는 `data-perf-row-from-tail`(끝에서 몇 번째)로 전체 행 수를 알아내
0번부터 끝까지 본다. 예전에는 읽은 것들 사이만 봐서 맨 위를 통째로 못 읽어도 "빠진
것 없음"으로 나왔다. 빠진 번호가 있으면 이웃 행의 위치를 보고 그 자리로 직접 가서 줍는다.
원본 대화를 감추는 것도 수집이 끝난 뒤로 미뤘다.

**0.1.4** — 실제 화면에서 나온 세 가지.

프롬프트가 아예 안 보이던 문제. 실제 화면에서는 사용자 메시지와 답변의 `entry-index` 가
서로 다르다(`human` 0, `assistant` 1, `human` 2 …). 그 번호로 샷을 묶다 보니 사용자 행이
혼자 샷이 되고 답변 쪽에는 프롬프트가 없어 말풍선이 안 그려졌다. 이제 번호가 아니라
**사용자 행이 나오면 새 샷이 시작하는 것**으로 묶는다. 모사 화면도 실제처럼 번호를
다르게 주도록 고쳐 회귀를 잡는다.

대화를 다 못 읽던 문제. 행 높이를 재는 동안 전체 높이가 계속 바뀌는데 처음 읽은 높이로
한 번만 훑고 끝냈다. 이제 바닥에 닿을 때까지 매번 높이를 다시 읽으며 돌고, 그러고도
빠진 번호가 남으면 절반 간격으로 한 번 더 훑는다. 다 읽으면 몇 개를 읽었는지,
못 읽은 줄이 있으면 몇 개인지 화면에 잠깐 띄운다.

스크롤바가 안 보이던 문제. 브라우저 기본 스크롤바는 OS 설정에 따라 자리를 아예 안 차지해서
(headless 측정값 0px) 직접 그리는 것으로 바꿨다. 끌어서 이동할 수 있고, 가운데 아래에
`4 / 21 · 4p` 형태로 지금 위치도 같이 나온다.

**0.1.3** — 세 가지.
프롬프트를 그 샷의 모든 페이지 위에 붙였다. 페이지를 넘겨도 같은 질문이 위에 남고
다음 샷에서 바뀐다. 휠을 한 칸씩 넘기던 것에서 연속 가로 스크롤로 바꾸고 멈추면
가장 가까운 페이지에 맞추게 했다. 트랙에 가로 스크롤바도 넣었다.
카드 폭은 원래 스크롤 화면의 글줄 폭을 읽어 거기에 맞춘다. 줄바꿈이 스크롤 화면과
같아지고, 카드보다 넓어서 잘리던 표 문제도 같이 없어졌다(경계 걸침 20줄 → 0줄).

**0.1.2** — 페이지끼리 구분되도록 카드에 얇은 선과 연한 그림자를 넣었다.
`box-decoration-break: clone` 덕에 그림자도 조각(페이지)마다 온전히 그려진다.
그림자 색은 화면 밝기를 보고 정해서 다크 모드에서도 보인다.

**0.1.1** — 페이지 보기에서 원본 대화가 비쳐 보이던 문제. 원인은 앱의 색 변수였다.
claude.ai 의 `--bg-100`, `--text-100` 등은 색이 아니라 HSL 숫자 세 개(`48 33.3% 97.1%`)라서
`background: var(--bg-100, #faf9f7)` 처럼 쓰면 변수가 정의되어 있으니 대체값이 안 먹고,
색으로는 무효라 배경이 투명해진다. 이제 실제로 그려진 색을 읽어서 쓴다(`src/source.js` 의 `theme()`).
여기에 더해 페이지 보기 동안에는 원본 대화를 규칙으로 감춘다(`html.cpv-on`). 자리는 그대로 두어
가상 스크롤이 계속 동작한다. 덮개 밖(사이드바·입력창)은 그대로 쓸 수 있다.

## 남은 것

- 창이 아주 좁으면 카드 폭이 글줄 폭보다 작아지고, 그때는 표가 카드 안에서 가로로
  스크롤된다. 가로 드래그 이동과 겹치는 조작이라 손봐야 한다
- 생성 중 표시를 앱의 신호(`data-perf-row-streaming`, `aria-busy`, shimmer)와 연결하기
- 수집이 끝나기 전에도 쓸 수 있게 점진 표시
- 수집에 27행 기준 10초쯤 걸린다. 더 줄일 여지가 있다
